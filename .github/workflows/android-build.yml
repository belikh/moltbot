name: Android Build

on:
  workflow_dispatch:  # Trigger manually from GitHub Actions tab
  push:
    branches:
      - main
      - 'android-*'

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          accept-android-sdk-licenses: false

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 9.2.1

      - name: Install Android SDK packages
        run: |
          yes | sdkmanager --licenses >/dev/null
          sdkmanager --install \
            "platform-tools" \
            "platforms;android-36" \
            "build-tools;36.0.0"

      - name: Grant execute permission for gradlew
        run: chmod +x apps/android/gradlew

      - name: Build Debug APK
        working-directory: apps/android
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: List APK output
        run: |
          echo "=== Listing APK output ==="
          find apps/android/app/build/outputs -name "*.apk" -type f 2>/dev/null || echo "No APKs found"
          ls -la apps/android/app/build/outputs/apk/ 2>/dev/null || echo "No apk directory"

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: moltbot-android-debug
          path: |
            apps/android/app/build/outputs/apk/debug/*.apk
            apps/android/app/build/outputs/apk/release/*.apk
          retention-days: 7
          if-no-files-found: warn
